{
  "name": "basiliska",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "pages": "node scripts/publish-gh-pages.mjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  },
  "description": "Basiliska — retro isometric MMORPG foundation. Valehaven gate town (Three.js + TypeScript)."
}
